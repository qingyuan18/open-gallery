apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: comfyui-cw-auth
  namespace: default
spec:
  podIdentity:
    provider: aws           # Use 'aws' for IRSA (IAM Roles for Service Accounts)
    identityOwner: workload # Use the workload's ServiceAccount IRSA role

