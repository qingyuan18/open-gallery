apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: comfyui-cw-auth
  namespace: default
spec:
  podIdentity:
    provider: aws           # Use 'aws' IRSA; operator mode uses keda-operator's SA
    identityOwner: keda     # Use the KEDA operator's ServiceAccount IRSA role

